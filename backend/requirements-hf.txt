# Single-container (Hugging Face Spaces) dependency set.
#
# Identical to requirements.txt minus sentence-transformers, which pulls torch
# and roughly 2 GB of wheels. That package is only the *embedding fallback* for
# when Ollama is unreachable -- and in this deployment Ollama runs inside the
# same container, so the fallback is unreachable by construction. Dropping it
# is a size decision, not a capability one.
#
# app/services/embeddings.py imports sentence_transformers lazily, so nothing
# breaks at import; if Ollama were somehow down, the review would fail loudly,
# which is the documented behaviour anyway.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20
numpy==1.26.4
pytest==8.3.4
