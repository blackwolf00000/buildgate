# Empty means "same origin": the UI calls the Next.js route handlers in
# app/api, which is how the hosted demo runs. Set it to http://localhost:8000
# to point the UI at the real FastAPI backend instead.
NEXT_PUBLIC_API_BASE_URL=
